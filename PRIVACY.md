# プライバシーポリシー

最終更新日: 2026-08-27

YouTube Archive Queue（以下「本拡張」）は、YouTubeチャンネルの動画をキューとして管理・連続再生するために、以下の情報を利用します。

## 取得・保存する情報

- YouTubeチャンネルの識別子、名前、画像URL
- 動画の識別子、タイトル、URL、サムネイルURL、配信日
- キュー内の順番
- 視聴済み・未視聴・除外状態
- 動画内の再生位置
- キューの作成・更新日時

これらは、ユーザーがYouTube上でキュー作成・更新・再生機能を利用した場合に取得されます。

## 利用目的

取得した情報は、次の機能を提供するためだけに使用します。

- 動画キューの作成、表示、並べ替え、検索
- 同じキュー内での連続再生
- 視聴状態と再生位置の保存・復元
- 保存済みキューとの差分更新
- ユーザー操作によるJSONバックアップ・復元

## 保存場所

情報は`chrome.storage.local`を使用して、ユーザーのブラウザー内に保存します。キューや視聴履歴などの利用データを、本拡張の開発者が運営する外部サーバーには送信しません。

管理ページを開いた際、更新の有無を確認するため`piniki-dev.github.io`から最新版情報（`version.json`）を取得します。この通信にキュー、視聴履歴、YouTubeの閲覧情報は含まれません。

JSONバックアップを実行した場合のみ、ユーザーが指定したダウンロード先へ保存データを書き出します。復元するファイルも、ユーザーが明示的に選択した場合にのみ読み込みます。

## 外部提供・広告・分析

本拡張は、保存データを第三者へ販売、共有、送信しません。広告、アクセス解析、行動追跡、クラッシュレポート用の外部サービスを使用しません。

YouTube動画の閲覧と再生に伴う通信は、通常のYouTube利用と同様にYouTubeとの間で行われ、Googleのプライバシーポリシーが適用されます。

## データの削除

キュー管理ページからチャンネル単位で保存データを削除できます。拡張機能をChromeから削除した場合も、Chromeが本拡張のローカル保存領域を削除します。

## 権限

- `storage`: キューと視聴進捗をブラウザー内に保存するため
- `unlimitedStorage`: 大量の動画と複数チャンネルを保存するため
- `https://www.youtube.com/*`: YouTubeのチャンネル一覧を読み取り、連続再生UIを提供するため
- `https://piniki-dev.github.io/youtube-archive-queue/*`: 最新版情報を確認し、更新を通知するため

## ポリシーの変更

データの取り扱いを変更する場合は、この文書とリリースノートを更新します。

## 問い合わせ

不具合やプライバシーに関する問い合わせは、GitHubリポジトリのIssuesで受け付けます。
